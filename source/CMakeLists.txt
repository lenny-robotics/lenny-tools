if (${LENNY_PROJECT}_BUILD_APPS)
    add_subdirectory(apps)
endif ()
if (${LENNY_PROJECT}_BUILD_TESTS)
    add_subdirectory(tests)
endif ()
if (NOT TARGET lenny::tools)
    add_subdirectory(tools)
endif ()